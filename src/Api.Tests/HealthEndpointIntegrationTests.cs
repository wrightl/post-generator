// Integration tests that hit the real API (e.g. /health) require a running app with configured dependencies.
// Run unit tests only in CI; for full integration tests, run locally with docker-compose and appsettings.
