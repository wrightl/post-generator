# The API image is built from the repo root so both Api and Core are in context.
# See ./Dockerfile.api (repo root) and azure.yaml (api.docker.path).
# To build: from repo root run: docker build -f Dockerfile.api -t postgen-api .
